# Galeon Environment Variables
#
# Each app/package has its own .env.example:
# - packages/contracts/.env.example  - Contract deployment
# - apps/api/.env.example            - Backend (Carlos)
# - apps/web/.env.example            - Frontend (if needed)
# - apps/indexer/.env.example        - Indexer (if needed)
#
# Copy each to .env in the same directory and fill in values.
